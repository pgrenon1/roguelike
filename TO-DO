
- Debug that allows us to cycle through available entities and we can add the selected one with mouse clicks
- Dynamically populate a 
- Load JSON once at runtime instead of each time

- DNA system
- Map generation
- 
- Inventory system?

Components : 
- Destructible 
- Item user
- Item picker
- AI_Behaviours
   -  Random walker
   -  Player seeker
   -  Player avoider





   # Scrolling camera notes : 
    - We need to pass all currently visible entities into an array
    - We need to restrain which entities enter this array : camera view
    - We need to update this camera as the player moves


   Needed attributes : 
   - Entities, position, characters
   - Should be updated in render processor 
   - 



   1. Fetch all entities to be rendered
   2. Store them all in a list.  